When trying to locate a folder the browser says 'your browser does not support folder selection, enter the folder path manually'
When selecting a folder that does not contain a git repo, no error is shown (creating a board)
Agent definitions need a folder on the hard drive when they should be stored in the db
Agentt components are not editable
Agents are assigned to columns in the agent view but this should be done in the column view
Tasks cannot be dragged to destination columns, and no indicator is shown for which columns are valid or invalid
When clicking on a feature in a column, it opens a popup and also filters the task columns. popup opening should happen only when an edit button is clicked
there should be a consistent menu navigation across the whole app
Users should be able to create features on the roadmap, and the roadmap should show a dependency graph of features
Dependencies should be visible on both the dependent and the dependee
tick boxes and their labels are misaligned
there is no way to unselect a feature and show all the board's steps
i've created a feature and a step, put both into 'ready', and created an agent for 'build' but i can't see that the step is being moved to build and then an agent assigned to it
when i moved the step into build then the ui shows an agent working on it, but i dont see copilot being triggered in my os, and there is no link to open the agent window
each ticket and task has a status indicator but no explanation. this should be visible when opening the popup view